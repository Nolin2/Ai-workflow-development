Customer Churn Prediction: AI Workflow
This repository applies the AI Development Workflow to predict customer churn. The primary goal is high Recall to minimize missed retention opportunities.

Key Components:
Model: Random Forest Classifier (class_weight='balanced') to handle data imbalance.

Artifacts: Saved model (.pkl) and scaler are deployed for consistent predictions.

Deployment: Interactive Streamlit dashboard (app.py).
